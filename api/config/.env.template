# Environment Configuration Template for Greenspot Grocer API
# Copy this file to .env and customize the values

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_NAME=greenspot_grocer
DB_PORT=3306

# API Configuration  
API_HOST=127.0.0.1
API_PORT=8000
CORS_ORIGINS=*

# JWT Configuration
JWT_SECRET_KEY=greenspot_grocer_secret_key_2025
JWT_EXPIRE_MINUTES=30

# Admin Credentials (Change these in production!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Master Key for Encryption (Keep this secret!)
GREENSPOT_MASTER_KEY=greenspot_default_key_2025

# Production Notes:
# 1. Change ADMIN_PASSWORD to a strong password
# 2. Generate a new JWT_SECRET_KEY (use: python -c "import secrets; print(secrets.token_urlsafe(32))")
# 3. Set a secure GREENSPOT_MASTER_KEY (keep this secret and backed up!)
# 4. Use specific CORS_ORIGINS instead of * in production
# 5. Consider using environment variable management tools like python-dotenv